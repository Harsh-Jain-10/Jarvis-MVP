# Jarvis-MVP
Jarvis-like voice assistant MVP built with Python/Flask. Uses local Ollama LLM for conversation (context memory enabled), free external APIs for real-time web facts, and pyautogui for local system control (open apps, screenshot, shutdown). Features a minimal, voice-first web UI.
